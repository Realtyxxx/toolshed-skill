# <tool> — <一句话定位>

<两三句：解决什么问题，什么时候该想起它。写场景，不写"它是一个 XX 工具"。>

- 入口：`~/.local/bin/<tool>`
- 配置：`~/.config/<tool>/config.toml`
- 数据 / 日志：`~/.<tool>/`（保留 N 天）
- 来源 / 装法：<AI 生成 / cargo install / 公司脚本 …>

## 工作原理

<只写"不看源码就想不到"的那部分：数据怎么流、谁调谁、为什么要这么绕。
显而易见的省略。有依赖假设（零依赖 / 需要 venv / 需要 root）在这里说。>

## 常用命令

```bash
<cmd> <sub>              # 干什么用，默认值陷阱写在这
<cmd> <sub> --flag       # 什么时候需要这个 flag
```

<如果有按领域分类的一大堆子命令，用表格；不要逐条抄 --help。>

## 自查

```bash
command -v <tool> && <tool> --version
```

<标题必须正好是 `## 自查`，其后的第一个 fenced block 必须明确标记为 bash。
默认 doctor 只静态校验；只有 `--run-checks` 才实际执行。本块属于受信任的本地代码，
只能放只读、秒回、不发网络请求的检查。>

## 注意事项

<全篇最有价值的一段。优先写：>

- **静默失败**：坏了但不报错的情况，以及怎么发现。
- **会被什么覆盖**：公司脚本、工具自身重写配置、另一个 agent 的安装流程。
- **默认截断 / 默认限额**：以为没匹配上其实是被截了。
- **改一处要同步改的另一处**：上游地址、端点、路径格式。
- **共享机器的坑**：PID 复用、端口抢占、/tmp 冲突。
- 密钥只写"从哪拿"，不写值。日期写绝对日期。
